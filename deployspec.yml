version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
  pre_build:
    commands:
      - echo Entered Deploy Stage
      - ls
      - echo installing npm
      - npm install
  build:
    commands:
      - cdk deploy

