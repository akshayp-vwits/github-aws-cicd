version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
      java: corretto8
  build:
    commands:
      - ls
      - cat versiontest.txt
artifacts:
  files:
    - '**/*'
  base-directory: artifacts
